# Run-card — crowd spectator for HoopClone (3d-master-modeler), 2026-07-29

| Phase | Proof required | Artifact / value | Status |
|---|---|---|---|
| 0 — Intake & routing | Framework, poly budget, format | Blender 5.2 bpy headless (already installed; the PyPI `bpy` wheel does NOT exist for this laptop's Python 3.12). Budget <= 500 tris, drawn ~700x through one MultiMesh. Deliverable: GLB + proof renders. | DONE |
| 1 — Blockout | Blockout render, dimensions checked | `fan_front34/side/top.png` pass 1. Seated height 1.088 m vs a ~1.10 m real seated adult. | DONE |
| 2 — Topology & refinement | Mesh audit | 194 quads / 28 tris / 4 ngons (flat caps only), **456 triangulated**, **0 non-manifold edges, 0 loose verts**, dims 0.585 x 0.660 x 1.122 m. UV layers `['Float2','UV2']` — UV2 carries the per-part animation tags. | DONE |
| 3 — PBR materials | Material list + sockets | Proof-render material only: Principled Base Color (0.42,0.45,0.52), Roughness 0.82. The GAME does not use it — Godot drives albedo per instance (shirt / trousers / skin) from the UV2 part tag plus per-instance custom data. | DONE |
| 3b — Photo-real textures | Sources applied | **N/A — deliberate.** The asset is instanced ~700x at 20-40 m; it carries no UVs for texturing and no texture would resolve. Colour variety is per-instance in the game shader instead. Photo PBR is being spent on the court and courtside, not the crowd. | DONE |
| 4 — Lighting & camera | Rig + test render | 3-point rig (warm key front-left, cool fill front-right at ~1/4 energy, white rim behind), 50 mm camera, Track-To an empty at the chest. Dark neutral world so the rig does the work. Proof: the three renders above. | DONE |
| 5 — Verification loop | Iterations + findings | **3 passes.** (1) Volumes butted edge-to-edge read as a pile of floating blocks — head buried, arms detached; fixed by overlapping every volume. (2) Still wrong: the figure was built facing +Y so all three cameras rendered its BACK, and the shoulder box swallowed the head; fixed by mirroring Y and slimming the shoulders. (3) Reads as a seated person: head on a visible neck, shoulders, arms into the lap, thighs forward, shins down, feet flat. Verified again **in the actual game at broadcast distance**, which is the real deliverable — not just the 1 m studio render. | DONE |
| Delivery | Files + sizes + rationale | `assets/models/crowd_fan.glb` **24 KB**; build script kept at `tools/models/build_fan.py` so the asset is reproducible, never hand-edited. GLB because it is Godot's native import and the mesh needs its second UV channel preserved; Draco was skipped — at 24 KB compression would cost more in decode setup than it saves. | DONE |